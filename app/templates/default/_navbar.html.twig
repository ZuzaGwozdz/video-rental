    <nav class="navbar navbar-expand-md navbar-light bg-light">
    <a class="navbar-brand" href="#">Menu</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav mr-auto">
            {% if is_granted('ROLE_USER') %}
            <li class="nav-item active">
                <a class="nav-link" href="{{ url('tape_index') }}">{{ 'label_tapes'|trans }}</a>
            </li>
            {% endif %}
            {% if is_granted('ROLE_USER') and not is_granted('ROLE_ADMIN') %}
            <li class="nav-item">
                <a class="nav-link" href="{{ url('reservation_index') }}">{{ 'label_reservations'|trans }}</a>
            </li>
            {% endif %}
            {% if is_granted('ROLE_ADMIN') %}
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    {{ 'label_admin'|trans }}
                </a>
                <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                    <a class="dropdown-item" href="{{ url('user_index') }}">{{ 'label_users'|trans }}</a>
                    <a class="dropdown-item" href="{{ url('category_index') }}">{{ 'label_categories'|trans }}</a>
                    <a class="dropdown-item" href="{{ url('tag_index') }}">{{ 'label_tags'|trans }}</a>
                    <div class="dropdown-divider"></div>
                    <a class="dropdown-item" href="{{ url('reservation_index') }}">{{ 'label_reservations'|trans }}</a>
                </div>
            </li>
            {% endif %}
        </ul>
        <ul class="navbar-nav">
            {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ url('account_index') }}">{{ 'title_account_index'|trans }}</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ url('app_logout') }}">{{ 'action_logout'|trans }}</a>
                </li>
            {% else %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ url('app_login') }}">{{ 'action_login'|trans }}</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ url('app_register') }}">{{ 'action_register'|trans }}</a>
                </li>
            {% endif %}
        </ul>
    </div>
</nav>
<br>