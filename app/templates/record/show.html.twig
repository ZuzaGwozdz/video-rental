{% extends 'base.html.twig' %}

{% block title %}
    View record
{% endblock %}

{% block body %}
    <h1>View record</h1>

    <a href="{{ url('record_index') }}">Homepage</a>

    {% if item is defined and item|length %}
        <dl>
            <p>{{ dump(item) }}<p>
            <dt>Title</dt>
            <dd>{{ item.title }}</dd>
            <dt>Content</dt>
            <dd>{{ item.content }}</dd>
            <dt>Tags</dt>
            <dd>{{ item.tags|join(', ') }}</dd>
        </dl>
    {% else %}
        <p>
            Record not found.
        </p>
    {% endif %}
{% endblock %}